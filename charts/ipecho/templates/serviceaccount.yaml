{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "ipecho.serviceAccountName" . }}
  labels:
    {{- include "ipecho.labels" . | nindent 4 }}
{{- end }}
